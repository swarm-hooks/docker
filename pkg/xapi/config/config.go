// Package config is for internal binary compatible conversion types
// between the external http types and the internal types that are
// handled by a backend (daemon or swarm manager, etc)
package config
